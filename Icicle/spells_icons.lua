--[[
    Spells' icon replacement
    To replace with item icon use GetItemIcon(itemId)
    To replace with spell icon use select(3, GetSpellInfo(spellId))
]]

IcicleSpellIcons = {
    [71607] = GetItemIcon(50354),
    [71646] = GetItemIcon(50354)
}